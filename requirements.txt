streamlit==1.34.0
langchain==0.1.10             # Pin to a slightly older, very stable LangChain version
langchain-community==0.0.10   # Pin to a compatible langchain-community (often needed transitively)
langchain-core==0.1.28        # Pin to a compatible, widely used langchain-core version
requests
pydantic==2.7.1
